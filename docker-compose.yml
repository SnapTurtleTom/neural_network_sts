version: '3.8'
services:
  cifar10-api:
    build: .
    container_name: cifar10-api
    ports:
      - "8000:8000"
    volumes:
      - ./model.pth:/app/model.pth
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
